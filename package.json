{
  "name": "aes-befe",
  "version": "1.0.0",
  "private": true,
  "description": "Unified Zig AES-256-CBC streaming encryption — Wasm (browser) + .so (Bun FFI)",
  "scripts": {
    "dev": "bash start-all.sh",
    "build:wasm": "cd zig-src && bash build.sh",
    "build:frontend": "cd frontend && bun run build"
  }
}
